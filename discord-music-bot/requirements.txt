# These packages are already installed in this Repl's Python environment via
# the package manager (.pythonlibs). This file is kept for reference / for
# running the bot outside Replit.
#
# yt-dlp is intentionally left unpinned to a max version — YouTube changes
# its cipher/player code frequently, and the bot also auto-updates it on
# every startup (see bot.py's _update_ytdlp). Always keep it at latest.
discord.py>=2.7.1
yt-dlp>=2026.7.4
davey>=0.1.6
PyNaCl>=1.6.2
Flask>=3.1.3
python-dotenv>=1.2.2
aiohttp>=3.14.1
async-timeout>=5.0.1

# System dependency (installed via Replit's Nix package manager, not pip):
# ffmpeg, libopus, libsodium — see replit.nix / .replit [nix] packages.
